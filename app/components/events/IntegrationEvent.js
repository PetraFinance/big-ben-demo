import React from 'react';
import { getEventPosition } from '../../helpers/position';
import { genObjectId } from '../../helpers/html';

export default class IntegrationEvent extends React.Component {

  render() {
    return (
      <div />
    );
  }
}
